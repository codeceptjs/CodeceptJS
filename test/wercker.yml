box:
  id: docker/compose:1.16.0
  cmd: /bin/sh
  entrypoint: /bin/bash -c

build:
  steps:
    - script:
      code: |
        echo "HELLO WORLD"

# test:
#   steps:
#     - script:
#       code: docker-compose run --rm test-unit
